{
  "name": "rutgersc-github-io",
  "version": "1.0.0",
  "description": "Local development server for GitHub Pages site",
  "private": true,
  "scripts": {
    "start": "npm run dev",
    "build": "node scripts/update-version.js && tsc",
    "watch": "tsc --watch",
    "dev": "npm run build && http-server -p 8000 -o youtube.html"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "typescript": "^5.3.0"
  }
}
